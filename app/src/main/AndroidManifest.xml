<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartechbraintechnologies.medillah">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Network.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medillah">
        <activity android:name=".Activities.ProductDescriptionActivity"/>
        <activity android:name=".Activities.MyLabTestDetailsActivity" />
        <activity android:name=".Activities.EditProfileActivity" />
        <activity android:name=".Activities.ContactUsActivity" />
        <activity android:name=".Network.NoNetworkActivity" />
        <activity android:name=".Activities.TestResultActivity" />
        <activity android:name=".Activities.TestDetails" />
        <activity android:name=".Activities.AppointmentDetailsActivity" />
        <activity android:name=".Activities.MyOrdersActivity" />
        <activity android:name=".Activities.ConfirmOrderActivity" />
        <activity android:name=".Activities.OrderDetailsActivity" />
        <activity android:name=".Activities.MyAppointmentsActivity" />
        <activity android:name=".Activities.MyLabTestsActivity" />
        <activity android:name=".Activities.ScheduleConsultationActivity" />
        <activity android:name=".Activities.DoctorDetailsActivity" />
        <activity android:name=".Activities.TestOrderConfirmationActivity" />
        <activity android:name=".Activities.ScheduleLabTestActivity" />
        <activity android:name=".Activities.LabTestDetailsActivity" />
        <activity android:name=".Activities.DoctorListActivity" />
        <activity android:name=".SearchEngine" />
        <activity android:name=".Activities.ProductDetailsActivity" />
        <activity android:name=".Activities.ProductListActivity" />
        <activity android:name=".Activities.ShoppingCartActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Activities.GetLocationActivity" />
        <activity android:name=".Activities.MyAddressesActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Authentication.ClientSurveyActivity" />
        <activity android:name=".Authentication.VerifyOtpActivity" />
        <activity android:name=".Authentication.PhoneAuthActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
    </application>

</manifest>